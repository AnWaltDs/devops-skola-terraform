# Answer

No secrets in code, variables with descriptions and defaults, outputs for useful values, credentials from env vars. This is a solid single-environment setup.

Next question: what happens when you need dev, test, and prod?
