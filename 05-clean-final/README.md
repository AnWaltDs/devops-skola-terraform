# Exercise 05 - Clean Final Version

This is the tidy reference version.

## What to do

1. Copy `terraform.tfvars.example` to `terraform.tfvars`
2. Fill in the lab credentials
3. Run `terraform init`
4. Run `terraform plan`
5. Run `terraform apply`

## Learning point

This is the version students should understand after the workshop.
