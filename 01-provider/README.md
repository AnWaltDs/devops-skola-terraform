# Exercise 01 - Add the Provider

## What to do

1. Add your lab credentials in a `terraform.tfvars` file, or export them as environment variables.
2. Run `terraform init`
3. Run `terraform plan`

## Learning point

The provider is Terraform's bridge to the cloud platform.
