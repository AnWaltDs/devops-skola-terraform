# Question

Compare `envs/dev/provider.tf` with `envs/test/provider.tf` and `envs/prod/provider.tf`. How many files are identical across the three folders?

What do you need to change to add a staging environment?
